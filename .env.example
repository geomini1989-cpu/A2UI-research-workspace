# DeepSeek API key — server-side ONLY. Never expose to the browser.
DEEPSEEK_API_KEY=sk-your-key-here

# Optional overrides
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
PORT=3001

# Research data provider. Only "demo" is registered today.
RESEARCH_PROVIDER=demo
